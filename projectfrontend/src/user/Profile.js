import React from "react";
import Base from "../core/Base";

const Profile = () => {
  return (
    <Base title="User Profile Page">
      <h1>This is the Profile Page</h1>
    </Base>
  );
};

export default Profile;
